#!/bin/bash

case "$1" in
"")
  echo "Please specify an URL"
  ;;
*)
  yt-dlp \
    --extract-audio --audio-format mp3 \
    --embed-thumbnail \
    --embed-metadata \
    --output "%(title)s.%(ext)s" \
    "${1}"
  ;;
esac

