###################
###  KEYBINDS   ###
###################

# it is there in case somebody wants to remap their keys quickly
$mainMod = SUPER
$altMod = ALT
$shift = SHIFT
$ctrl = CTRL

$ipc = qs -c noctalia-shell ipc call
$menu = $ipc launcher toggle
$altMenu = rofi -show drun

$terminal = kitty -1
$fileManager = dolphin
$browser = zen-browser

# Hyprland Specific
bind = $mainMod, Q, killactive,
bind = $mainMod $shift, Q, killactive, 1
bind = $mainMod, W, togglefloating,
bind = $mainMod, P, pseudo,
bind = $mainMod, i, togglesplit,
bind = $mainMod $shift, F, fullscreen
bind = $mainMod, F, fullscreen, 1
bind = $mainMod, grave, togglespecialworkspace, special
bind = $mainMod $shift, grave, movetoworkspace, special
bind = $mainMod, F1, exec, ~/.config/hypr/scripts/util.sh toggle-anim
bind = $mainMod $shift, P, pin

# groups
bind = $mainMod, G, togglegroup
bind = $mainMod $shift, G, lockactivegroup, toggle
bind = $mainMod, tab, changegroupactive, f
bind = $mainMod $shift, tab, changegroupactive, b
bind = $mainMod $ctrl, H, moveintogroup, l
bind = $mainMod $ctrl, L, moveintogroup, r
bind = $mainMod $ctrl, K, moveintogroup, u
bind = $mainMod $ctrl, J, moveintogroup, d
bind = $mainMod, D, moveoutofgroup

# Launch Defaults
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, E, exec, $fileManager
bind = $altMod, Space, exec, $menu
bind = $mainMod, R, exec, $altMenu
bind = $mainMod, Z, exec, $browser
bind = $mainMod, O, exec, $ipc lockScreen lock

# Tools
bind = $mainMod, C, exec, hyprpicker -a

# Wall Changer
# bind = $mainMod, Y, exec, qs -c noctalia-shell ipc call wallpaper toggle
bind = $mainMod, Y, exec, qs -c noctalia-shell ipc call plugin:wallcards toggle

# Screenshot
# bind = , PRINT, exec, hyprshot -z -m region -o /home/$USER/Pictures/Screenshots # Regular screenshot
bind = , PRINT, exec, quickshell -c HyprQuickFrame -n
bind = $shift, PRINT, exec, $ipc plugin:screen-toolkit annotate

# Clipboard Managers
bind = $mainMod, V, exec, qs -c noctalia-shell ipc call launcher clipboard
bind = $mainMod $shift, V, exec, cliphist wipe # Wipe clipboard

# Noctalia Shell Menus
bind = $mainMod, PERIOD, exec, qs -c noctalia-shell ipc call launcher emoji
bind = $altMod, F4, exec, qs -c noctalia-shell ipc call sessionMenu toggle

# Hide / Show Bar
bind = $mainMod $shift, W, exec, qs -c noctalia-shell ipc call bar toggle

# Overview plugin https://github.com/yayuuu/hyprland-scroll-overview
# bind = SUPER, a, scrolloverview:overview, toggle

# Playerctl noctalia
bindel = , XF86AudioRaiseVolume, exec, $ipc volume increase
bindel = , XF86AudioLowerVolume, exec, $ipc volume decrease
bindl = , XF86AudioMute, exec, $ipc volume muteOutput
bindel = , XF86MonBrightnessUp, exec, $ipc brightness increase
bindel = , XF86MonBrightnessDown, exec, $ipc brightness decrease

###########
# DWINDLE #
###########

bind = $mainMod, up, movefocus, u
bind = $mainMod, right, movefocus, r
bind = $mainMod, left, movefocus, l
bind = $mainMod, down, movefocus, d

# Grow/shrink windows (resize)
binde = $mainMod $altMod, left, resizeactive,-70 0
binde = $mainMod $altMod, right, resizeactive,70 0
binde = $mainMod $altMod, up, resizeactive,0 -70
binde = $mainMod $altMod, down, resizeactive,0 70

bind = $mainMod $shift, up, movewindow, u
bind = $mainMod $shift, right, movewindow, r
bind = $mainMod $shift, left, movewindow, l
bind = $mainMod $shift, down, movewindow, d

# Grow/shrink window sizes with VIM keybinds
binde = $mainMod $altMod, h, resizeactive,-70 0
binde = $mainMod $altMod, l, resizeactive,70 0
binde = $mainMod $altMod, k, resizeactive,0 -70
binde = $mainMod $altMod, j, resizeactive,0 70

bind = $mainMod $shift, k, movewindow, u
bind = $mainMod $shift, l, movewindow, r
bind = $mainMod $shift, h, movewindow, l
bind = $mainMod $shift, j, movewindow, d

bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod $shift, 1, movetoworkspace, 1
bind = $mainMod $shift, 2, movetoworkspace, 2
bind = $mainMod $shift, 3, movetoworkspace, 3
bind = $mainMod $shift, 4, movetoworkspace, 4
bind = $mainMod $shift, 5, movetoworkspace, 5
bind = $mainMod $shift, 6, movetoworkspace, 6
bind = $mainMod $shift, 7, movetoworkspace, 7
bind = $mainMod $shift, 8, movetoworkspace, 8
bind = $mainMod $shift, 9, movetoworkspace, 9
bind = $mainMod $shift, 0, movetoworkspace, 10

bind = $mainMod $ctrl, 1, movetoworkspacesilent, 1
bind = $mainMod $ctrl, 2, movetoworkspacesilent, 2
bind = $mainMod $ctrl, 3, movetoworkspacesilent, 3
bind = $mainMod $ctrl, 4, movetoworkspacesilent, 4
bind = $mainMod $ctrl, 5, movetoworkspacesilent, 5
bind = $mainMod $ctrl, 6, movetoworkspacesilent, 6
bind = $mainMod $ctrl, 7, movetoworkspacesilent, 7
bind = $mainMod $ctrl, 8, movetoworkspacesilent, 8
bind = $mainMod $ctrl, 9, movetoworkspacesilent, 9
bind = $mainMod $ctrl, 0, movetoworkspacesilent, 10
