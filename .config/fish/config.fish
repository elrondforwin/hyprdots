# this config is like that because if I cat this thing in zsh the caelestia theme loads slow, this fixes it without feeling the launch difference.
# I use zsh as my default shell but launch fish with foot terminal to load the theme correctly.
cat ~/.local/state/caelestia/sequences.txt 2>/dev/null
zsh
